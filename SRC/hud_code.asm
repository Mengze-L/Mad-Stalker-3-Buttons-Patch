; Archived build.ps1 wrapper for the code-cave payload only.
; Normal builds assemble ..\patch.asm as a sparse S-record through build.bat.

        include "ports.inc"
        include "ram_addrs.inc"
        include "hud_symbols.inc"
        include "hud_code.inc"
